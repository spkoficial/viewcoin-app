# Usado apenas quando você roda comandos do @workspace/db diretamente,
# como `pnpm --filter @workspace/db run push`.
# Copie para ".env" e preencha com a conexão do seu PostgreSQL local.
DATABASE_URL=
